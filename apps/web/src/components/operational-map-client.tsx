"use client";

import { useEffect, useMemo, useRef, useState } from "react";
import { Circle, CircleMarker, MapContainer, TileLayer, Tooltip, useMap } from "react-leaflet";
import { demoReferencePoints, projectOperationalPoints, type OperationalPoint } from "@/lib/demo-locations";
import { useDemoScenario } from "./demo-scenario-store";

type Filter = "ALL" | "AFFECTED" | "ENVIRONMENT" | "INCIDENT";
type Sort = "SEVERITY" | "AQI" | "PM2.5" | "INCIDENT" | "LOCATION";

function aqiColor(aqi: number): string { if (aqi <= 50) return "#16a34a"; if (aqi <= 100) return "#ca8a04"; if (aqi <= 150) return "#ea580c"; if (aqi <= 200) return "#dc2626"; if (aqi <= 300) return "#7e22ce"; return "#7f1d1d"; }
function incidentRank(point: OperationalPoint): number { if (point.incidentStatus === "Kebakaran sintetis") return 3; if (point.incidentStatus === "Asap sintetis") return 2; return 0; }
function severity(point: OperationalPoint): number { return point.aqi + incidentRank(point) * 200 + (point.environmentalStatus === "Perlu verifikasi" ? 30 : 0); }

function FitPoints({ points }: { points: readonly OperationalPoint[] }) { const map = useMap(); const hasFitted = useRef(false); useEffect(() => { if (hasFitted.current) return; map.fitBounds(points.map((point) => point.coordinates), { padding: [32, 32], maxZoom: 10, animate: false }); hasFitted.current = true; }, [map, points]); return null; }
function ResizeForSidebar() { const map = useMap(); useEffect(() => { const resize = () => map.invalidateSize({ animate: false }); window.addEventListener("lumi-sidebar-resize", resize); window.addEventListener("resize", resize); return () => { window.removeEventListener("lumi-sidebar-resize", resize); window.removeEventListener("resize", resize); }; }, [map]); return null; }
function FocusSelectedPoint({ point, request, activeRadiusMeters }: { point: OperationalPoint; request: number; activeRadiusMeters: number | null }) { const map = useMap(); useEffect(() => { if (!request) return; const openPopup = () => { const content = document.createElement("div"); const title = document.createElement("strong"); title.textContent = point.name; content.append(title, document.createElement("br"), `${point.kelurahan}, ${point.kecamatan}`, document.createElement("br"), `AQI ${point.aqi} · PM2.5 ${point.pm25} µg/m³`); map.openPopup(content, point.coordinates, { autoPan: false }); }; map.once("moveend", openPopup); const zoom = activeRadiusMeters ? activeRadiusMeters > 25_000 ? 10 : activeRadiusMeters > 10_000 ? 11 : 12 : 13; map.flyTo(point.coordinates, zoom, { animate: true, duration: .45 }); const fallback = window.setTimeout(openPopup, 700); return () => { window.clearTimeout(fallback); map.off("moveend", openPopup); }; }, [activeRadiusMeters, map, point, request]); return null; }
function FullscreenButton({ target }: { target: React.RefObject<HTMLDivElement | null> }) { const [isFullscreen, setFullscreen] = useState(false); useEffect(() => { const onChange = () => setFullscreen(document.fullscreenElement === target.current); document.addEventListener("fullscreenchange", onChange); return () => document.removeEventListener("fullscreenchange", onChange); }, [target]); const toggle = async () => { if (!target.current) return; if (document.fullscreenElement) await document.exitFullscreen(); else await target.current.requestFullscreen(); }; return <button className="map-fullscreen" type="button" onClick={() => void toggle()} aria-label={isFullscreen ? "Tutup layar penuh" : "Buka peta layar penuh"} title={isFullscreen ? "Tutup layar penuh" : "Layar penuh"}>⛶</button>; }

export function OperationalMapClient() {
  const { scenario, role } = useDemoScenario(); const [query, setQuery] = useState(""); const [filter, setFilter] = useState<Filter>("ALL"); const [sort, setSort] = useState<Sort>("SEVERITY"); const [selectedId, setSelectedId] = useState(demoReferencePoints[0]!.id); const [focusRequest, setFocusRequest] = useState(0); const frame = useRef<HTMLDivElement>(null); const points = useMemo(() => projectOperationalPoints(scenario), [scenario]); const selected = points.find((point) => point.id === selectedId) ?? points[0]!; const activeSources = scenario.workflow === "READY" ? [] : scenario.simulation.sources; const selectedSource = activeSources.find((source) => source.sourcePointId === selected.id);
  const visible = useMemo(() => { const needle = query.trim().toLocaleLowerCase("id-ID"); return points.filter((point) => { const matchesText = !needle || [point.name, point.province, point.kabupaten, point.kecamatan, point.kelurahan].join(" ").toLocaleLowerCase("id-ID").includes(needle); const matchesFilter = filter === "ALL" || (filter === "AFFECTED" && point.affected) || (filter === "ENVIRONMENT" && point.environmentalStatus !== "Tervalidasi") || (filter === "INCIDENT" && point.incidentStatus !== "Tidak ada"); return matchesText && matchesFilter; }).sort((left, right) => { if (sort === "AQI") return right.aqi - left.aqi; if (sort === "PM2.5") return right.pm25 - left.pm25; if (sort === "INCIDENT") return incidentRank(right) - incidentRank(left) || right.aqi - left.aqi; if (sort === "LOCATION") return left.kabupaten.localeCompare(right.kabupaten, "id"); return severity(right) - severity(left); }); }, [filter, points, query, sort]);
  const affected = points.filter((point) => point.affected).length; const roleAction = role === "DLH" ? "Validasi lingkungan tersedia pada alur insiden." : "Verifikasi dan respons BPBD tersedia pada alur insiden."; const selectPoint = (id: string) => { setSelectedId(id); setFocusRequest((current) => current + 1); };
  return <section className="operational-map" aria-labelledby="operational-map-title"><header className="operational-map-head"><div><p className="eyebrow">Peta operasional · Kalimantan Barat</p><h2 id="operational-map-title">50 titik referensi data uji</h2><p className="muted">AQI dan PM2.5 adalah nilai sintetis untuk demo lokal, bukan pemantauan atau prediksi resmi.</p></div><span className="badge tier-verify">{affected} titik terdampak</span></header><div className="operational-map-grid"><div ref={frame} className="operational-map-frame"><FullscreenButton target={frame} /><MapContainer center={[-0.35, 110.6]} zoom={7} minZoom={5} maxZoom={13} scrollWheelZoom className="operational-map-canvas" aria-label="Peta operasional data uji Kalimantan Barat"><TileLayer attribution='&copy; <a href="https://www.openstreetmap.org/copyright">OpenStreetMap</a> contributors' url="https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png" /><FitPoints points={points} /><FocusSelectedPoint point={selected} request={focusRequest} activeRadiusMeters={selectedSource?.radiusMeters ?? null} /><ResizeForSidebar />{activeSources.map((source) => <Circle key={source.id} center={demoReferencePoints.find((point) => point.id === source.sourcePointId)?.coordinates ?? selected.coordinates} radius={source.radiusMeters} pathOptions={{ color: "#ea580c", fillColor: "#f59e0b", fillOpacity: .12, weight: 2 }} />)}{points.map((point) => <CircleMarker key={point.id} center={point.coordinates} radius={point.id === selected.id ? 12 : 8} pathOptions={{ color: point.id === selected.id ? "#0f172a" : point.incidentStatus === "Tidak ada" ? "#ffffff" : "#7f1d1d", fillColor: aqiColor(point.aqi), fillOpacity: 1, weight: point.id === selected.id ? 4 : point.incidentStatus === "Tidak ada" ? 2 : 5 }} eventHandlers={{ click: () => selectPoint(point.id) }}><Tooltip>{point.name} · AQI {point.aqi}{point.incidentStatus !== "Tidak ada" ? ` · ${point.incidentStatus}` : ""}</Tooltip></CircleMarker>)}</MapContainer><span className="map-data-label">Visualisasi lokal · data sintetis</span><div className="aqi-legend" aria-label="Legenda AQI"><strong>AQI</strong><span><i className="aqi-good" />0–50</span><span><i className="aqi-moderate" />51–100</span><span><i className="aqi-sensitive" />101–150</span><span><i className="aqi-unhealthy" />151–200</span><span><i className="aqi-very-unhealthy" />201–300</span><span><i className="aqi-hazardous" />301+</span><em>Cincin gelap = insiden sintetis</em></div></div></div><div className="operational-map-tools"><label>Cari titik atau wilayah<input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Nama, kabupaten, kecamatan…" /></label><label>Filter<select value={filter} onChange={(event) => setFilter(event.target.value as Filter)}><option value="ALL">Semua titik</option><option value="AFFECTED">Terdampak simulasi</option><option value="ENVIRONMENT">Perlu perhatian lingkungan</option><option value="INCIDENT">Ada insiden sintetis</option></select></label><label>Urutkan<select value={sort} onChange={(event) => setSort(event.target.value as Sort)}><option value="SEVERITY">Paling berat</option><option value="AQI">AQI tertinggi</option><option value="PM2.5">PM2.5 tertinggi</option><option value="INCIDENT">Status insiden</option><option value="LOCATION">Lokasi</option></select></label></div><aside className="operational-point-panel"><div className="operational-point-detail"><div><p className="eyebrow">Titik dipilih</p><h3>{selected.name}</h3><p className="muted">{selected.kelurahan}, {selected.kecamatan} · {selected.kabupaten}, {selected.province}</p></div><dl><div><dt>AQI</dt><dd style={{ color: aqiColor(selected.aqi) }}>{selected.aqi}</dd></div><div><dt>PM2.5</dt><dd>{selected.pm25} µg/m³</dd></div><div><dt>Tren</dt><dd>{selected.trend}</dd></div><div><dt>Status</dt><dd>{selected.incidentStatus}</dd></div></dl><p className="freshness">{roleAction}</p></div><div className="operational-point-list" aria-label="Daftar titik operasional">{visible.map((point) => <button key={point.id} className={point.id === selected.id ? "is-selected" : ""} type="button" onClick={() => selectPoint(point.id)}><i aria-hidden style={{ background: aqiColor(point.aqi) }} /><span className="operational-point-copy"><strong>{point.name}</strong><small>{point.kecamatan}, {point.kabupaten}</small></span><span className="operational-point-metrics"><b>AQI {point.aqi}</b><small>PM2.5 {point.pm25}</small></span></button>) || <p className="muted">Tidak ada titik yang cocok.</p>}</div></aside></section>;
}
